{
    "title": "Justia Lawyer Directory Scraper",
    "description": "Scrapes attorney profiles from Justia.com lawyer directory listings by state and practice area.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrl": {
            "title": "Start URL",
            "type": "string",
            "description": "Justia lawyer directory listing URL (e.g. https://www.justia.com/lawyers/personal-injury/kansas)",
            "editor": "textfield",
            "prefill": "https://www.justia.com/lawyers/personal-injury/kansas"
        },
        "maxLawyers": {
            "title": "Max Lawyers",
            "type": "integer",
            "description": "Maximum number of lawyer profiles to scrape (0 = unlimited)",
            "default": 0,
            "minimum": 0
        },
        "maxListingPages": {
            "title": "Max Listing Pages",
            "type": "integer",
            "description": "Maximum number of listing pages to crawl (0 = unlimited)",
            "default": 0,
            "minimum": 0
        },
        "stopAtListingTotal": {
            "title": "Stop at listing total",
            "type": "boolean",
            "description": "Stop once unique profiles reach the parsed directory total (true matches come first; the rest is padding up to a ~1,000-item server cap) and report reachedListingEnd=true. Also detects Justia's sparse-combo fallback — a practice-area URL served as the broader state/city hub (canonical drops the PA segment) — and reports it as a verified-zero listing (listingTotal=0) instead of crawling ~25 pages of padding. Disable only to audit the padding itself.",
            "default": true
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Apify proxy configuration. US residential proxies required for Justia (Cloudflare).",
            "editor": "proxy",
            "default": {
                "useApifyProxy": true,
                "apifyProxyGroups": ["RESIDENTIAL"],
                "apifyProxyCountry": "US"
            }
        }
    },
    "required": ["startUrl"]
}
